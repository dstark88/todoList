<br>
<center>
  <h1><strong>Busy To Do List</strong></h1>
  <h2>Get Busy Getting Stuff Done!</h2>
</center>

<div class=container>
  <div class=row>
    <div class=col-md-1></div>
    <div class=col-md-5><strong>
      <center>
      <h2><strong>To Do List!</strong></h2>
        <ul>
          {{#each todos}}
          {{#if done}}
          {{> todos/todo-block doit=false}}
          {{/if}}
          {{/each}}
        </ul>
      </center>
    </div>
    <div class=col-md-1></div>
    <div class=col-md-5>
      <center>
      <h2><strong>My Done List!</strong></h2>
        <ul>
          {{#each todos}}
          {{#unless done}}
          {{> todos/todo-block doit=true}}
          {{/unless}}
          {{/each}}
        </ul></strong>
    </center> 
    </div>
  </div>
</div>

<center><strong>
  <h2>Add List Item</h2>
  <form class="create-form">

    <div class="form-group">
      <label for="ca">Description:</label>
      <input type="text" id="ca" name="name">
    </div>

    <div class="form-group">
      <label for="done">To Do or Done?</label><br>
      <input type="radio" name="done" value="1" checked> To Do<br>
      <input type="radio" name="done" value="0"> Done
    </div>

    <button class="btn btn-secondary btn-sm" type="submit">Submit</button>
  </form></strong>
</center>